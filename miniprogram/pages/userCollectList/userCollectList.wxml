        <view class="page__hd" style="{{collect.length==0?'':'display:none'}}">
            <view class="page__desc">您还没有添加错题，请先开始听写吧！</view>
        </view>

        <view class="weui-cells weui-cells_after-title">
            <view class="weui-cell" wx:for='{{collect}}'>
                <view class="weui-cell__bd">{{item.name}}</view>
                <view class="weui-cell__ft">{{item.count}}次</view>
            </view>
        </view>